# ✨ Content AI Assistant

Production-grade веб-приложение на базе **Streamlit** и **GigaChat API** для автоматизации контент-маркетинга. Приложение позволяет генерировать контент-планы, посты для социальных сетей, лид-магниты (с автоматической версткой в PDF) и SEO-статьи.

## 🛠 Стек технологий

* **Frontend & Backend:** Python 3.10+, Streamlit (с использованием `st.navigation` для Single Page Application).
* **Дизайн:** Кастомный CSS (Glassmorphism / Liquid Glass).
* **ИИ:** GigaChat-2-Max (SDK `gigachat`).
* **Валидация данных:** Pydantic (строгий парсинг JSON).
* **Генерация PDF:** ReportLab Platypus (динамическая верстка Markdown в PDF "на лету").
* **Безопасность:** `streamlit-authenticator` (хэширование паролей, secure cookies).

## 🗂 Архитектура проекта

Проект построен по принципу строгого разделения бизнес-логики и интерфейса (Separation of Concerns):
- `src/views/` — только UI-компоненты Streamlit.
- `src/services/` — интеграции (LLM, PDF) и независимая бизнес-логика.
- `src/models/` — схемы данных Pydantic.
- `assets/` — промпты, шрифты и стили.

## 🚀 Локальный запуск (Local Development)

### 1. Подготовка файлов
Убедитесь, что вы положили необходимые файлы в папку `assets/`:
- **Промпты:** Положите ваши `.txt` файлы промптов в `assets/prompts/` (`content_plan.txt`, `broadcasts.txt`, `lead_magnets.txt`, `seo_articles.txt`).
- **Шрифты:** Скачайте `Roboto-Regular.ttf` и `Roboto-Bold.ttf` и положите в `assets/fonts/` (необходимо для генерации PDF).

### 2. Настройка окружения
Создайте файл `.env` в корне проекта (для учетных данных входа):
```env
SETTINGS_USERNAME=admin
SETTINGS_PASSWORD=super_secret_password
AUTH_COOKIE_KEY=random_secure_string_12345